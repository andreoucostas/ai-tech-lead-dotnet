{
  "template": "dotnet",
  "version": "0.12.0",
  "applied": "2026-06-04",
  "_comment": "Machine-readable framework version stamp. Bumped when this repo syncs template updates. CI tooling (and future /framework-update command) reads this file to detect drift between the consumer repo and the latest template version. Keep in sync with the HTML comment at the top of CLAUDE.md."
}

{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "command": "bash -c 'if [[ \"$CLAUDE_FILE_PATH\" == *.cs ]]; then dotnet build --no-restore --verbosity quiet 2>&1 | tail -20; fi'"
      }
    ]
  }
}

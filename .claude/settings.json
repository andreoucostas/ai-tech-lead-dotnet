{
  "_comment": "Hooks run via PowerShell 7 (pwsh) using the .ps1 twins — cross-platform and, crucially, not dependent on bash being on PATH (the bash default silently no-opped on Windows .NET boxes). The installer adapts this to the machine: scripts/install.ps1 falls back to Windows PowerShell 5.1 (settings.windows.json) when pwsh is absent; scripts/install.sh switches to the bash twins. Edit CLAUDE.md for rules, not this file.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "pwsh -NoProfile -ExecutionPolicy Bypass -File .claude/hooks/session-start.ps1" }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          { "type": "command", "command": "pwsh -NoProfile -ExecutionPolicy Bypass -File .claude/hooks/route-prompt.ps1" }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          { "type": "command", "command": "pwsh -NoProfile -ExecutionPolicy Bypass -File .claude/hooks/guard.ps1" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          { "type": "command", "command": "pwsh -NoProfile -ExecutionPolicy Bypass -File .claude/hooks/post-write.ps1" },
          { "type": "command", "command": "pwsh -NoProfile -ExecutionPolicy Bypass -File .claude/hooks/audit-trail.ps1" }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          { "type": "command", "command": "pwsh -NoProfile -ExecutionPolicy Bypass -File .claude/hooks/boy-scout-check.ps1" }
        ]
      }
    ]
  }
}

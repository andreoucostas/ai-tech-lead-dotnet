# What We've Learned

> Long-form observations the team has captured while using the AI Tech Lead framework on this codebase.
> Append entries; do not overwrite. Prefer dates in `YYYY-MM-DD` format.
> Format: `[YYYY-MM-DD] observation — what worked, what didn't, what rule we changed as a result.`

_No entries yet. As the team uses this framework, record what works, what causes friction, and what rules need adjusting._
